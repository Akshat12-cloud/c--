#include<iostream>
#include<string>
#include<vector>
using namespace std;
int main(){
    string ch="let's start string";
    int length=ch.length();
    int s=0;
    int e=length;
    while(s<=e){
        swap(ch[s],ch[e]);      
    }
    int newstart;
    int newend;
    for(int i=0;i<ch.length();i++){
        newnend++;
        if(ch[i] != )
    }
}